apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: demo-app
  namespace: kargo
spec:
  subscriptions:
  - image:
      repoURL: ghcr.io/kargobook/demo-app
      semverConstraint: ">=1.0.0 <2.0.0"
